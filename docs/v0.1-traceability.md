# v0.1 traceability checklist

Maps every acceptance claim of the v0.1 umbrella
([#1](https://github.com/photon-circus/ph-surfaces/issues/1)) to the issue
that implemented it, the tests that check it, the documentation section that
states it, and the `scripts/ci.sh` gate that enforces it. This file is
repository material; it is not part of the packaged crate (the `include`
allowlist in `Cargo.toml` and the exact file-set assertion in the
`package build` check keep `docs/` out of the artifact).

Test names are the Rust `fn` names. `src/…` tests are unit tests;
`tests/conformance/…` tests are the black-box suite, which goes through the
public API only and compares against an independent `i128` reference. Doc
sections refer to `README.md` headings and the crate-level rustdoc in
`src/lib.rs` (`cargo doc`).

Nothing in this file is a claim of measurement: no cycle counts, WCET, code
size, or device accuracy are asserted anywhere in the crate.

## Child issue disposition

| Issue | Title | Disposition |
| --- | --- | --- |
| #2 | Bootstrap an independent core-only Rust crate | Closed, merged |
| #3 | Private deterministic integer segment interpolation | Closed, merged |
| #4 | Validated static `BilinearSurface` representation | Closed, merged |
| #5 | Binary axis lookup and four-sided boundary handling | Closed, merged |
| #6 | Deterministic X-then-Y bilinear evaluation | Closed, merged |
| #7 | Numerical and boundary conformance suite | Closed, merged |
| #8 | Dependency, `no_std`, no-alloc, storage, work bounds | Closed, merged |
| #9 | Documentation, examples, traceability, package readiness | This gate |

Closing #9 is sufficient to unblock closure of #1. Publishing the crate,
creating a release or tag, and declaring a stable 1.0 API are separate
maintainer actions and are not performed by any child issue.

## Frozen contract: public representation

| Claim in #1 | Issue | Tests | Documentation | CI gate |
| --- | --- | --- | --- | --- |
| Public concrete type is `BilinearSurface<const NX, const NY>` | #4 | `src/surface.rs::two_by_two_surface_declares_as_a_static` | README "Contract › Representation"; rustdoc `BilinearSurface` | `check`, `doc` |
| References `&'static [u16; NX]`, `&'static [u16; NY]`, row-major `&'static [[i32; NX]; NY]`, addressed `values[y][x]` | #4 | `src/surface.rs::values_are_addressed_row_major_by_y_then_x`, `accessors_return_the_declared_tables_without_copying` | README "Representation"; rustdoc `BilinearSurface` § Orientation | `test` |
| Axis/grid shape mismatch is a compile-time type error, not a runtime error | #4 | `compile_fail` doctest on `BilinearSurface` (transposed grid) | README "Representation"; rustdoc `BilinearSurface` | `test` (doctests) |
| `const fn new` asserts ≥ 2 knots per axis and strict increase; invalid constants fail compilation | #4 | `compile_fail` doctests on `BilinearSurface::new` (one knot, empty axis, duplicate, descending, per axis); `src/surface.rs::{zero,one}_knot_{x,y}_axis_is_rejected`, `duplicate_{x,y}_knots_are_rejected`, `descending_{x,y}_knots_are_rejected`, `a_valid_x_axis_does_not_mask_an_invalid_y_axis`, `a_descending_interior_x_step_is_rejected` | README "Representation"; rustdoc `BilinearSurface::new` § Panics | `test` |
| All four sides default to Error; named const policy/builder sets each side independently | #4 | `src/surface.rs::new_defaults_every_side_to_error`, `all_sixteen_boundary_combinations_are_representable`, `the_sixteen_combinations_are_pairwise_distinct`; `src/boundary.rs::default_policy_rejects_every_side`, `builders_are_usable_in_const_context`, `each_builder_method_changes_only_its_own_side` | README "Boundary policies and errors"; rustdoc `Boundary`, `BoundaryPolicy` | `test` |
| No units, provenance, achieved-error, host report, or generated metadata in the runtime handle | #4 | `src/surface.rs::handle_is_three_references_plus_four_policy_bytes_and_padding`, `handle_size_is_independent_of_table_size` | README "Representation", "Resource accounting and cost"; rustdoc § Resource accounting | `test` |

## Frozen contract: boundary and error behaviour

| Claim in #1 | Issue | Tests | Documentation | CI gate |
| --- | --- | --- | --- | --- |
| Error results distinguish X-below, X-above, Y-below, Y-above and report coordinate and bound | #4, #5 | `src/error.rs::each_side_reports_its_coordinate_and_bound`, `sides_are_not_equal_to_each_other`, `display_names_the_axis_the_direction_and_the_bound`; `tests/conformance/boundary.rs::{x,y}_{below,above}_under_error_reports_the_coordinate_and_the_{first,last}_{x,y}_knot` | README "Boundary policies and errors"; rustdoc `SurfaceError` | `test` |
| Clamp substitutes the nearest endpoint; extrapolation is never performed | #5, #6 | `src/lookup.rs::clamping_never_extrapolates`, `{x,y}_{below,above}_clamp_resolves_to_the_{first,last}_cell`; `tests/conformance/boundary.rs::clamping_never_extrapolates`, `{x,y}_{below,above}_under_clamp_evaluates_the_{first,last}_{x,y}_{column,row}` | README "Boundary policies and errors"; rustdoc `Boundary::Clamp`, `evaluate` § Domain | `test` |
| X resolved first, then Y; X error wins when both are outside Error sides | #5, #6 | `src/evaluate.rs::error_error_corners_report_the_x_side`; `tests/conformance/boundary.rs::error_error_corners_report_the_x_side`, `error_clamp_corners_still_report_the_x_side`, `a_y_first_precedence_would_fail_on_every_error_error_corner` | README "Precedence: X before Y" (doctest); rustdoc § Contract, `evaluate` § Domain | `test` |
| If X clamps, Y is still evaluated under its own policy | #6 | `src/evaluate.rs::a_clamped_x_does_not_suppress_a_y_error`, `a_clamped_y_does_not_suppress_an_x_error`; `tests/conformance/boundary.rs::a_clamped_x_is_followed_by_a_y_resolved_under_its_own_selection`, `a_clamped_y_never_masks_an_x_error`, `clamp_error_corners_report_the_y_side` | README "Precedence: X before Y" (doctest); rustdoc `evaluate` § Domain | `test` |
| Every one of the sixteen policies behaves per side, and in-domain results are policy independent | #5, #6 | `src/evaluate.rs::all_sixteen_policies_agree_in_domain`, `each_side_selects_independently_of_the_other_three`; `tests/conformance/boundary.rs::all_sixteen_policies_agree_with_the_reference_on_every_region`, `a_policy_selects_only_what_happens_outside_the_domain`, `an_error_side_never_returns_a_value_and_a_clamp_side_never_returns_an_error` | README "Boundary policies and errors" | `test` |
| Domain endpoints are inclusive on every side | #5 | `src/lookup.rs::both_endpoint_knots_resolve_exactly`, `endpoints_are_accepted_under_every_policy`; `tests/conformance/boundary.rs::the_domain_endpoints_are_inclusive_on_every_side` | rustdoc `SurfaceError` variant docs ("inclusive … bound") | `test` |

## Frozen contract: numerical behaviour

| Claim in #1 | Issue | Tests | Documentation | CI gate |
| --- | --- | --- | --- | --- |
| Each segment computes the exact rational `y0*(span-offset) + y1*offset` over `span` in `i64` | #3 | `src/interp.rs::agrees_with_independent_reference`, `endpoints_return_exact_endpoint_values`, `increasing_decreasing_and_flat_segments`, `positive_negative_and_zero_crossing_ranges` | README "Scalar rounding"; rustdoc § Contract › Rounding | `test` |
| Division rounds to nearest; exact half-way rounds away from zero | #3, #7 | `src/interp.rs::div_round_half_away_from_zero_rounds_to_nearest_without_ties`, `…_rounds_exact_ties_away`, `…_is_odd`, `positive_half_way_values_round_away_from_zero`, `negative_half_way_values_round_away_from_zero`; `tests/conformance/scalar.rs::positive_and_negative_half_way_values_round_away_from_zero`, `a_tie_at_the_final_y_step_also_rounds_away_from_zero`, `negating_the_grid_negates_every_result_exactly`, `ties_toward_zero_would_fail_on_named_points` | README "Scalar rounding" (doctest with `TIES`); rustdoc § Contract › Rounding | `test` |
| Bilinear = X on lower-Y row, X on upper-Y row, then Y between the two rounded values | #6 | `src/evaluate.rs::the_locked_order_fixture_returns_the_x_then_y_value`, `half_way_values_round_away_from_zero_through_the_composition`; `tests/conformance/order.rs::the_locked_order_fixture_returns_the_x_then_y_value`, `the_locked_order_fixture_agrees_with_the_reference_everywhere` | README "Normative X-then-Y bilinear order"; rustdoc § Evaluation contract, `evaluate` § Order | `test` |
| The order is observable and normative: axes `[0, 2]`, rows `[[0, 0], [1, 3]]`, `(1, 1)` → `1` (Y-then-X → `2`) | #6, #7 | `src/evaluate.rs::the_locked_order_fixture_discriminates_between_the_two_orders`; `tests/conformance/order.rs::the_locked_order_fixture_rejects_y_then_x`, `every_rounding_fixture_contains_points_that_discriminate_the_order`, `planar_and_row_shifted_fixtures_are_order_blind_and_the_suite_knows_it` | README locked-fixture doctest; rustdoc crate example, `evaluate` doctest | `test` |
| Valid full-range operands cannot overflow: weights sum to `span ≤ 65_535`, numerator `< 2^47`, result in the `i32` hull; no public overflow variant | #3, #6, #7 | `src/interp.rs::division_stays_exact_at_the_documented_numerator_bound`, `maximum_span_covers_the_full_u16_domain`, `extreme_i32_endpoints_in_both_directions`, `results_stay_in_the_endpoint_convex_hull`; `src/evaluate.rs::extreme_operands_evaluate_without_overflow`; `tests/conformance/range.rs::the_extreme_corners_return_their_stored_values`, `every_full_range_result_stays_inside_the_hull_of_its_cell`, `full_width_endpoints_and_neighbours_agree_with_the_reference`, `axes_containing_zero_and_u16_max_as_knots_evaluate_their_end_cells_exhaustively` | README "No arithmetic-overflow variant"; rustdoc § No arithmetic-overflow variant, `SurfaceError` (not `#[non_exhaustive]`), `evaluate` § Cost | `test` |
| Exact-knot, nonuniform, signed, half-way, boundary, corner, and full-range cases pass against an independent reference | #7 | `tests/conformance/scalar.rs::every_stored_point_of_every_fixture_returns_exactly`, `a_hand_computable_plane_is_exact_at_every_declared_point`, `a_nonuniform_grid_agrees_with_the_reference_over_its_whole_domain`, `the_inset_domain_agrees_with_the_reference_exhaustively`, `rows_of_every_sign_and_slope_compose_correctly`, `a_flat_column_and_decreasing_columns_compose_correctly`; `tests/conformance/lookup.rs::*_locates_against_the_linear_oracle*`; `tests/conformance/range.rs::a_sampled_sweep_of_the_full_range_agrees_with_the_reference` (sampled, not exhaustive) | README "How it is verified" | `test`, `integer only` (suite may not use float or `ph-curves` as an oracle) |

## Frozen contract: resource behaviour

| Claim in #1 | Issue | Tests | Documentation | CI gate |
| --- | --- | --- | --- | --- |
| Exact referenced element payload is `2*NX + 2*NY + 4*NX*NY` bytes | #8 | `src/surface.rs::referenced_payload_matches_the_documented_formula` | README "Resource accounting and cost › Storage"; rustdoc § Resource accounting, `BilinearSurface` § Storage | `test` |
| The formula excludes the handle (three references + policy + padding), alignment, linker, code, stack, flash; never called total memory | #8, #9 | `src/surface.rs::handle_is_three_references_plus_four_policy_bytes_and_padding`, `handle_size_is_independent_of_table_size` | Same sections — every statement of the formula is followed by "only the referenced element payload … not total RAM, flash, binary, or linker cost" | `test`; wording reviewed in #9 |
| Worst case is two logarithmic axis searches and three scalar interpolations; concrete comparison bound documented; no cycle/WCET claim | #5, #6, #8 | `src/lookup.rs::a_full_lookup_costs_two_endpoint_comparisons_plus_the_probes`, `the_search_probe_count_is_exact_and_data_independent`, `the_probe_bound_matches_the_documented_formula`, `an_out_of_domain_coordinate_never_searches`, `binary_lookup_costs_far_fewer_comparisons_than_a_scan` | README "Resource accounting and cost › Work"; rustdoc § Evaluation cost, `evaluate` § Cost ("not a cycle count or a WCET figure") | `test` |
| Evaluation is stateless: no warm-up, reset, cache, clock, I/O, persistence, hardware, or lifecycle behaviour | #6, #7 | `src/evaluate.rs::evaluation_is_free_of_warm_up_behaviour`, `repeated_calls_are_identical_and_mutate_no_state`; `tests/conformance/determinism.rs::repeated_evaluation_returns_identical_results`, `a_fresh_handle_answers_exactly_as_a_warmed_one`, `a_copy_of_the_handle_evaluates_identically_and_the_original_is_unchanged`, `interleaving_other_coordinates_does_not_change_a_result`, `errors_are_as_repeatable_as_values` | README "Stateless"; rustdoc § Statelessness, `evaluate` § Cost | `test` |

## Acceptance bar

| Acceptance item in #1 | Evidence |
| --- | --- |
| Every child is closed or has an explicit accepted disposition | Table above; #9 closes with this checklist |
| Package and every dependency kind remain free of `ph-curves` | Empty `[dependencies]`, `[dev-dependencies]`, `[build-dependencies]`; `deny.toml` ban; gates `no ph-curves` (manifest text, `Cargo.lock`, `cargo metadata --all-features`), `manifest floor` (empty tables), `deny`, `package build` (consumer lockfile names only two packages), `guards fire on mutation` (`ph-curves` case). README "Independence from `ph-curves`"; rustdoc § Runtime guarantees and independence |
| Unconditional `no_std`, core-only/no-allocation, integer-only, and target claims mechanically proven | Gates `no_std unconditional`, `integer only`, `core-only thumbv7em-none-eabi`, `core-only riscv32imac-unknown-none-elf`, `thumbv7em-none-eabi`, `riscv32imac-unknown-none-elf`, `guards fire on mutation` (`no_std-conditional`, `alloc` cases); `src/lib.rs` `#![no_std]`, `#![forbid(unsafe_code)]` |
| Public API implements the frozen representation, boundary, rounding, precedence, and X-then-Y contracts | Contract tables above |
| Exact-knot, nonuniform, signed, half-way, boundary, corner, full-range cases pass against an independent reference | Numerical table above; `tests/conformance/reference.rs` (`i128`, linear scan, remainder-based rounding, `i32::try_from` narrowing) |
| The two unrelated device-neutral fixtures compile from the packaged artifact | `ELEVATION` and `CORRECTION`: `tests/conformance/examples.rs::*` (exhaustive over each declared domain, hand-computed points, mutant disagreement); README "Examples" doctests and rustdoc § Examples doctests, both run from the unpacked package by `package build`; the downstream `#![no_std]` consumer in `package build` declares both, is tested on the host, and is built for both embedded targets |
| Storage and operation costs stated without overstating total memory or timing | Resource table above; wording rule recorded in `AGENTS.md` § 3a |
| Formatting, clippy, rustdoc, tests, dependency policy, package inspection, and downstream no-std smoke gates pass | `./scripts/ci.sh`: `fmt`, `clippy` (`-D warnings`), `doc` (`-D warnings`, `#![deny(missing_docs)]`), `test`, `no ph-curves`, `deny`, `package list`, `package build` |

## Issue #9 acceptance evidence

| Evidence required by #9 | Where |
| --- | --- |
| README examples and doctests compile from the packaged artifact | `src/lib.rs` `readme_doctests` (`cfg(doctest)`, `include_str!("../README.md")`); `package build` runs `cargo test --doc` and `cargo doc` with warnings denied inside the unpacked `.crate` |
| Rustdoc builds with warnings denied and all public items documented | `doc` gate (`RUSTDOCFLAGS='-D warnings'`); `#![deny(missing_docs)]` in `src/lib.rs` |
| Package contains no host-only, agent, CI, or design material | `Cargo.toml` `include` allowlist; `package list` rejects `AGENTS.md`, `CHANGELOG.md`, `clippy.toml`, `deny.toml`, `rust-toolchain.toml`, `scripts/`, `docs/`, `tests/`, `.github/`; `package build` diffs the archive against the exact expected file list |
| A fresh downstream no-std consumer can declare and evaluate both documented fixtures | `package build` consumer: `#![no_std]`, both maps as statics with policies, host `cargo test` on the documented points, builds for both embedded targets |
| Documentation never calls the payload formula total memory/binary cost, never calls a sampled domain exhaustive | README "Storage" and "How it is verified"; rustdoc § Resource accounting; `tests/conformance/range.rs` module doc ("sampled, never enumerated"). Reviewed by hand in #9 |
| Every child of #1 closed or dispositioned; every parent acceptance item has evidence | This file |
| Closing #9 unblocks #1; publication/tagging is a separate maintainer action | Stated here and in README "What state it is in" |

## Explicit v0.1 non-goals (kept out)

Documented in README "Out of scope" and rustdoc § Not in v0.1: no `ph-curves`
dependency or shared arithmetic crate; no inverse lookup; no other
dimensions, axis widths, or output types; no scattered points, irregular
meshes, bicubic interpolation, extrapolation, or fitting; no dynamic grids,
mutation, caching, allocation, `unsafe`, or floating point; no runtime
metadata, units, provenance, or generated reports; no host generation, CLI,
formula ingestion, or fitting; no device-specific equations, catalogs,
filtering, fusion, scheduling, buses, GPIO, async, or storage. Host-side
generation and a shared interpolation crate remain deferred follow-ups per #1.

## Not proven, not claimed

Every Rust target other than the host and the two representative bare-metal
targets; Xtensa; cycle counts; code-size ceilings; hard real-time WCET; total
RAM, flash, or binary cost; exhaustive coverage of the full `u16 × u16` range
(it is sampled under a stated rule; only the small declared domains are
enumerated exhaustively); any device, vendor, sensor, or measurement accuracy.
